// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: knowledge/engine/v1/engine.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "knowledge.engine.v1";

export enum DiagnosticSeverity {
  DIAGNOSTIC_SEVERITY_UNSPECIFIED = 0,
  DIAGNOSTIC_SEVERITY_ERROR = 1,
  DIAGNOSTIC_SEVERITY_WARNING = 2,
  DIAGNOSTIC_SEVERITY_INFORMATION = 3,
  DIAGNOSTIC_SEVERITY_HINT = 4,
  UNRECOGNIZED = -1,
}

export function diagnosticSeverityFromJSON(object: any): DiagnosticSeverity {
  switch (object) {
    case 0:
    case "DIAGNOSTIC_SEVERITY_UNSPECIFIED":
      return DiagnosticSeverity.DIAGNOSTIC_SEVERITY_UNSPECIFIED;
    case 1:
    case "DIAGNOSTIC_SEVERITY_ERROR":
      return DiagnosticSeverity.DIAGNOSTIC_SEVERITY_ERROR;
    case 2:
    case "DIAGNOSTIC_SEVERITY_WARNING":
      return DiagnosticSeverity.DIAGNOSTIC_SEVERITY_WARNING;
    case 3:
    case "DIAGNOSTIC_SEVERITY_INFORMATION":
      return DiagnosticSeverity.DIAGNOSTIC_SEVERITY_INFORMATION;
    case 4:
    case "DIAGNOSTIC_SEVERITY_HINT":
      return DiagnosticSeverity.DIAGNOSTIC_SEVERITY_HINT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DiagnosticSeverity.UNRECOGNIZED;
  }
}

export function diagnosticSeverityToJSON(object: DiagnosticSeverity): string {
  switch (object) {
    case DiagnosticSeverity.DIAGNOSTIC_SEVERITY_UNSPECIFIED:
      return "DIAGNOSTIC_SEVERITY_UNSPECIFIED";
    case DiagnosticSeverity.DIAGNOSTIC_SEVERITY_ERROR:
      return "DIAGNOSTIC_SEVERITY_ERROR";
    case DiagnosticSeverity.DIAGNOSTIC_SEVERITY_WARNING:
      return "DIAGNOSTIC_SEVERITY_WARNING";
    case DiagnosticSeverity.DIAGNOSTIC_SEVERITY_INFORMATION:
      return "DIAGNOSTIC_SEVERITY_INFORMATION";
    case DiagnosticSeverity.DIAGNOSTIC_SEVERITY_HINT:
      return "DIAGNOSTIC_SEVERITY_HINT";
    case DiagnosticSeverity.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface GetCapabilitiesRequest {
  workspaceInstanceId: string;
}

export interface GetCapabilitiesResponse {
  protocolVersion: string;
  engineVersion: string;
  capabilities: string[];
  storage: StorageCapabilities | undefined;
}

export interface StorageCapabilities {
  metadataStore: string;
  searchIndex: string;
  blobStore: boolean;
}

export interface ShutdownRequest {
  reason: string;
}

export interface ShutdownResponse {
  accepted: boolean;
}

export interface OpenWorkspaceRequest {
  indexPath: string;
}

export interface OpenWorkspaceResponse {
  ok: boolean;
  documents: string;
}

export interface CloseWorkspaceRequest {
}

export interface CloseWorkspaceResponse {
  ok: boolean;
}

export interface HasDocumentsRequest {
}

export interface HasDocumentsResponse {
  hasDocuments: boolean;
}

export interface RebuildIndexRequest {
  documents: WorkspaceDocument[];
}

export interface RebuildIndexResponse {
  documents: string;
}

export interface UpsertDocumentRequest {
  document: WorkspaceDocument | undefined;
}

export interface UpsertDocumentResponse {
  documents: string;
}

export interface RemoveDocumentRequest {
  path: string;
}

export interface RemoveDocumentResponse {
  documents: string;
}

export interface RemovePathPrefixRequest {
  prefix: string;
}

export interface RemovePathPrefixResponse {
  documents: string;
}

export interface WorkspaceDocument {
  path: string;
  title: string;
  content: string;
}

export interface SyncRequest {
  workspaceInstanceId: string;
  open?: OpenDocument | undefined;
  change?: ApplyDocumentChange | undefined;
  close?: CloseDocument | undefined;
  resync?: ResyncDocument | undefined;
}

export interface SyncResponse {
  acknowledged?: DocumentAcknowledged | undefined;
  diagnostics?: DiagnosticsChanged | undefined;
  resyncRequired?: ResyncRequired | undefined;
}

export interface OpenDocument {
  documentId: string;
  uri: string;
  version: string;
  content: string;
}

export interface ApplyDocumentChange {
  documentId: string;
  baseVersion: string;
  version: string;
  changes: TextEdit[];
}

export interface CloseDocument {
  documentId: string;
}

export interface ResyncDocument {
  documentId: string;
  version: string;
  content: string;
}

export interface DocumentAcknowledged {
  documentId: string;
  version: string;
}

export interface DiagnosticsChanged {
  documentId: string;
  version: string;
  diagnostics: Diagnostic[];
}

export interface ResyncRequired {
  documentId: string;
  reason: string;
}

export interface Diagnostic {
  range: Range | undefined;
  ruleId: string;
  message: string;
  severity: DiagnosticSeverity;
}

export interface GetDocumentSymbolsRequest {
  documentId: string;
  documentVersion: string;
}

export interface GetDocumentSymbolsResponse {
  symbols: MarkdownDocumentSymbol[];
}

export interface MarkdownDocumentSymbol {
  name: string;
  kind: number;
  level: number;
  slug: string;
  range: Range | undefined;
}

export interface GetLinksRequest {
  documentId: string;
  documentVersion: string;
}

export interface GetLinksResponse {
  links: MarkdownLink[];
}

export interface MarkdownLink {
  sourceDocumentId: string;
  text: string;
  target: string;
  range: Range | undefined;
  isExternal: boolean;
}

export interface SearchRequest {
  query: string;
  limit: number;
}

export interface SearchResponse {
  results: SearchResult[];
  done: boolean;
}

export interface SearchResult {
  documentId: string;
  path: string;
  title: string;
  line: number;
  column: number;
  endColumn: number;
  snippet: string;
  snippetHighlights: SearchHighlight[];
  score: number;
}

export interface SearchHighlight {
  start: number;
  end: number;
}

export interface TextEdit {
  range: Range | undefined;
  text: string;
}

export interface Range {
  start: Position | undefined;
  end: Position | undefined;
}

export interface Position {
  line: number;
  character: number;
}

function createBaseGetCapabilitiesRequest(): GetCapabilitiesRequest {
  return { workspaceInstanceId: "" };
}

export const GetCapabilitiesRequest: MessageFns<GetCapabilitiesRequest> = {
  encode(message: GetCapabilitiesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workspaceInstanceId !== "") {
      writer.uint32(10).string(message.workspaceInstanceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetCapabilitiesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetCapabilitiesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workspaceInstanceId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetCapabilitiesRequest {
    return {
      workspaceInstanceId: isSet(object.workspaceInstanceId)
        ? globalThis.String(object.workspaceInstanceId)
        : isSet(object.workspace_instance_id)
        ? globalThis.String(object.workspace_instance_id)
        : "",
    };
  },

  toJSON(message: GetCapabilitiesRequest): unknown {
    const obj: any = {};
    if (message.workspaceInstanceId !== "") {
      obj.workspaceInstanceId = message.workspaceInstanceId;
    }
    return obj;
  },

  create(base?: DeepPartial<GetCapabilitiesRequest>): GetCapabilitiesRequest {
    return GetCapabilitiesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetCapabilitiesRequest>): GetCapabilitiesRequest {
    const message = createBaseGetCapabilitiesRequest();
    message.workspaceInstanceId = object.workspaceInstanceId ?? "";
    return message;
  },
};

function createBaseGetCapabilitiesResponse(): GetCapabilitiesResponse {
  return { protocolVersion: "", engineVersion: "", capabilities: [], storage: undefined };
}

export const GetCapabilitiesResponse: MessageFns<GetCapabilitiesResponse> = {
  encode(message: GetCapabilitiesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.protocolVersion !== "") {
      writer.uint32(10).string(message.protocolVersion);
    }
    if (message.engineVersion !== "") {
      writer.uint32(18).string(message.engineVersion);
    }
    for (const v of message.capabilities) {
      writer.uint32(26).string(v!);
    }
    if (message.storage !== undefined) {
      StorageCapabilities.encode(message.storage, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetCapabilitiesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetCapabilitiesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.protocolVersion = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.engineVersion = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.capabilities.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.storage = StorageCapabilities.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetCapabilitiesResponse {
    return {
      protocolVersion: isSet(object.protocolVersion)
        ? globalThis.String(object.protocolVersion)
        : isSet(object.protocol_version)
        ? globalThis.String(object.protocol_version)
        : "",
      engineVersion: isSet(object.engineVersion)
        ? globalThis.String(object.engineVersion)
        : isSet(object.engine_version)
        ? globalThis.String(object.engine_version)
        : "",
      capabilities: globalThis.Array.isArray(object?.capabilities)
        ? object.capabilities.map((e: any) => globalThis.String(e))
        : [],
      storage: isSet(object.storage) ? StorageCapabilities.fromJSON(object.storage) : undefined,
    };
  },

  toJSON(message: GetCapabilitiesResponse): unknown {
    const obj: any = {};
    if (message.protocolVersion !== "") {
      obj.protocolVersion = message.protocolVersion;
    }
    if (message.engineVersion !== "") {
      obj.engineVersion = message.engineVersion;
    }
    if (message.capabilities?.length) {
      obj.capabilities = message.capabilities;
    }
    if (message.storage !== undefined) {
      obj.storage = StorageCapabilities.toJSON(message.storage);
    }
    return obj;
  },

  create(base?: DeepPartial<GetCapabilitiesResponse>): GetCapabilitiesResponse {
    return GetCapabilitiesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetCapabilitiesResponse>): GetCapabilitiesResponse {
    const message = createBaseGetCapabilitiesResponse();
    message.protocolVersion = object.protocolVersion ?? "";
    message.engineVersion = object.engineVersion ?? "";
    message.capabilities = object.capabilities?.map((e) => e) || [];
    message.storage = (object.storage !== undefined && object.storage !== null)
      ? StorageCapabilities.fromPartial(object.storage)
      : undefined;
    return message;
  },
};

function createBaseStorageCapabilities(): StorageCapabilities {
  return { metadataStore: "", searchIndex: "", blobStore: false };
}

export const StorageCapabilities: MessageFns<StorageCapabilities> = {
  encode(message: StorageCapabilities, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.metadataStore !== "") {
      writer.uint32(10).string(message.metadataStore);
    }
    if (message.searchIndex !== "") {
      writer.uint32(18).string(message.searchIndex);
    }
    if (message.blobStore !== false) {
      writer.uint32(24).bool(message.blobStore);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StorageCapabilities {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStorageCapabilities();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.metadataStore = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.searchIndex = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.blobStore = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StorageCapabilities {
    return {
      metadataStore: isSet(object.metadataStore)
        ? globalThis.String(object.metadataStore)
        : isSet(object.metadata_store)
        ? globalThis.String(object.metadata_store)
        : "",
      searchIndex: isSet(object.searchIndex)
        ? globalThis.String(object.searchIndex)
        : isSet(object.search_index)
        ? globalThis.String(object.search_index)
        : "",
      blobStore: isSet(object.blobStore)
        ? globalThis.Boolean(object.blobStore)
        : isSet(object.blob_store)
        ? globalThis.Boolean(object.blob_store)
        : false,
    };
  },

  toJSON(message: StorageCapabilities): unknown {
    const obj: any = {};
    if (message.metadataStore !== "") {
      obj.metadataStore = message.metadataStore;
    }
    if (message.searchIndex !== "") {
      obj.searchIndex = message.searchIndex;
    }
    if (message.blobStore !== false) {
      obj.blobStore = message.blobStore;
    }
    return obj;
  },

  create(base?: DeepPartial<StorageCapabilities>): StorageCapabilities {
    return StorageCapabilities.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StorageCapabilities>): StorageCapabilities {
    const message = createBaseStorageCapabilities();
    message.metadataStore = object.metadataStore ?? "";
    message.searchIndex = object.searchIndex ?? "";
    message.blobStore = object.blobStore ?? false;
    return message;
  },
};

function createBaseShutdownRequest(): ShutdownRequest {
  return { reason: "" };
}

export const ShutdownRequest: MessageFns<ShutdownRequest> = {
  encode(message: ShutdownRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reason !== "") {
      writer.uint32(10).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShutdownRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShutdownRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ShutdownRequest {
    return { reason: isSet(object.reason) ? globalThis.String(object.reason) : "" };
  },

  toJSON(message: ShutdownRequest): unknown {
    const obj: any = {};
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create(base?: DeepPartial<ShutdownRequest>): ShutdownRequest {
    return ShutdownRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ShutdownRequest>): ShutdownRequest {
    const message = createBaseShutdownRequest();
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseShutdownResponse(): ShutdownResponse {
  return { accepted: false };
}

export const ShutdownResponse: MessageFns<ShutdownResponse> = {
  encode(message: ShutdownResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accepted !== false) {
      writer.uint32(8).bool(message.accepted);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShutdownResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShutdownResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.accepted = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ShutdownResponse {
    return { accepted: isSet(object.accepted) ? globalThis.Boolean(object.accepted) : false };
  },

  toJSON(message: ShutdownResponse): unknown {
    const obj: any = {};
    if (message.accepted !== false) {
      obj.accepted = message.accepted;
    }
    return obj;
  },

  create(base?: DeepPartial<ShutdownResponse>): ShutdownResponse {
    return ShutdownResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ShutdownResponse>): ShutdownResponse {
    const message = createBaseShutdownResponse();
    message.accepted = object.accepted ?? false;
    return message;
  },
};

function createBaseOpenWorkspaceRequest(): OpenWorkspaceRequest {
  return { indexPath: "" };
}

export const OpenWorkspaceRequest: MessageFns<OpenWorkspaceRequest> = {
  encode(message: OpenWorkspaceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.indexPath !== "") {
      writer.uint32(10).string(message.indexPath);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OpenWorkspaceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOpenWorkspaceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.indexPath = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OpenWorkspaceRequest {
    return {
      indexPath: isSet(object.indexPath)
        ? globalThis.String(object.indexPath)
        : isSet(object.index_path)
        ? globalThis.String(object.index_path)
        : "",
    };
  },

  toJSON(message: OpenWorkspaceRequest): unknown {
    const obj: any = {};
    if (message.indexPath !== "") {
      obj.indexPath = message.indexPath;
    }
    return obj;
  },

  create(base?: DeepPartial<OpenWorkspaceRequest>): OpenWorkspaceRequest {
    return OpenWorkspaceRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<OpenWorkspaceRequest>): OpenWorkspaceRequest {
    const message = createBaseOpenWorkspaceRequest();
    message.indexPath = object.indexPath ?? "";
    return message;
  },
};

function createBaseOpenWorkspaceResponse(): OpenWorkspaceResponse {
  return { ok: false, documents: "0" };
}

export const OpenWorkspaceResponse: MessageFns<OpenWorkspaceResponse> = {
  encode(message: OpenWorkspaceResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    if (message.documents !== "0") {
      writer.uint32(16).uint64(message.documents);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OpenWorkspaceResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOpenWorkspaceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.documents = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OpenWorkspaceResponse {
    return {
      ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false,
      documents: isSet(object.documents) ? globalThis.String(object.documents) : "0",
    };
  },

  toJSON(message: OpenWorkspaceResponse): unknown {
    const obj: any = {};
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    if (message.documents !== "0") {
      obj.documents = message.documents;
    }
    return obj;
  },

  create(base?: DeepPartial<OpenWorkspaceResponse>): OpenWorkspaceResponse {
    return OpenWorkspaceResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<OpenWorkspaceResponse>): OpenWorkspaceResponse {
    const message = createBaseOpenWorkspaceResponse();
    message.ok = object.ok ?? false;
    message.documents = object.documents ?? "0";
    return message;
  },
};

function createBaseCloseWorkspaceRequest(): CloseWorkspaceRequest {
  return {};
}

export const CloseWorkspaceRequest: MessageFns<CloseWorkspaceRequest> = {
  encode(_: CloseWorkspaceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CloseWorkspaceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCloseWorkspaceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): CloseWorkspaceRequest {
    return {};
  },

  toJSON(_: CloseWorkspaceRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<CloseWorkspaceRequest>): CloseWorkspaceRequest {
    return CloseWorkspaceRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<CloseWorkspaceRequest>): CloseWorkspaceRequest {
    const message = createBaseCloseWorkspaceRequest();
    return message;
  },
};

function createBaseCloseWorkspaceResponse(): CloseWorkspaceResponse {
  return { ok: false };
}

export const CloseWorkspaceResponse: MessageFns<CloseWorkspaceResponse> = {
  encode(message: CloseWorkspaceResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CloseWorkspaceResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCloseWorkspaceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CloseWorkspaceResponse {
    return { ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false };
  },

  toJSON(message: CloseWorkspaceResponse): unknown {
    const obj: any = {};
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    return obj;
  },

  create(base?: DeepPartial<CloseWorkspaceResponse>): CloseWorkspaceResponse {
    return CloseWorkspaceResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CloseWorkspaceResponse>): CloseWorkspaceResponse {
    const message = createBaseCloseWorkspaceResponse();
    message.ok = object.ok ?? false;
    return message;
  },
};

function createBaseHasDocumentsRequest(): HasDocumentsRequest {
  return {};
}

export const HasDocumentsRequest: MessageFns<HasDocumentsRequest> = {
  encode(_: HasDocumentsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HasDocumentsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHasDocumentsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): HasDocumentsRequest {
    return {};
  },

  toJSON(_: HasDocumentsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<HasDocumentsRequest>): HasDocumentsRequest {
    return HasDocumentsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<HasDocumentsRequest>): HasDocumentsRequest {
    const message = createBaseHasDocumentsRequest();
    return message;
  },
};

function createBaseHasDocumentsResponse(): HasDocumentsResponse {
  return { hasDocuments: false };
}

export const HasDocumentsResponse: MessageFns<HasDocumentsResponse> = {
  encode(message: HasDocumentsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hasDocuments !== false) {
      writer.uint32(8).bool(message.hasDocuments);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HasDocumentsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHasDocumentsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.hasDocuments = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HasDocumentsResponse {
    return {
      hasDocuments: isSet(object.hasDocuments)
        ? globalThis.Boolean(object.hasDocuments)
        : isSet(object.has_documents)
        ? globalThis.Boolean(object.has_documents)
        : false,
    };
  },

  toJSON(message: HasDocumentsResponse): unknown {
    const obj: any = {};
    if (message.hasDocuments !== false) {
      obj.hasDocuments = message.hasDocuments;
    }
    return obj;
  },

  create(base?: DeepPartial<HasDocumentsResponse>): HasDocumentsResponse {
    return HasDocumentsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HasDocumentsResponse>): HasDocumentsResponse {
    const message = createBaseHasDocumentsResponse();
    message.hasDocuments = object.hasDocuments ?? false;
    return message;
  },
};

function createBaseRebuildIndexRequest(): RebuildIndexRequest {
  return { documents: [] };
}

export const RebuildIndexRequest: MessageFns<RebuildIndexRequest> = {
  encode(message: RebuildIndexRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.documents) {
      WorkspaceDocument.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RebuildIndexRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRebuildIndexRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.documents.push(WorkspaceDocument.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RebuildIndexRequest {
    return {
      documents: globalThis.Array.isArray(object?.documents)
        ? object.documents.map((e: any) => WorkspaceDocument.fromJSON(e))
        : [],
    };
  },

  toJSON(message: RebuildIndexRequest): unknown {
    const obj: any = {};
    if (message.documents?.length) {
      obj.documents = message.documents.map((e) => WorkspaceDocument.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<RebuildIndexRequest>): RebuildIndexRequest {
    return RebuildIndexRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RebuildIndexRequest>): RebuildIndexRequest {
    const message = createBaseRebuildIndexRequest();
    message.documents = object.documents?.map((e) => WorkspaceDocument.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRebuildIndexResponse(): RebuildIndexResponse {
  return { documents: "0" };
}

export const RebuildIndexResponse: MessageFns<RebuildIndexResponse> = {
  encode(message: RebuildIndexResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.documents !== "0") {
      writer.uint32(8).uint64(message.documents);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RebuildIndexResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRebuildIndexResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.documents = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RebuildIndexResponse {
    return { documents: isSet(object.documents) ? globalThis.String(object.documents) : "0" };
  },

  toJSON(message: RebuildIndexResponse): unknown {
    const obj: any = {};
    if (message.documents !== "0") {
      obj.documents = message.documents;
    }
    return obj;
  },

  create(base?: DeepPartial<RebuildIndexResponse>): RebuildIndexResponse {
    return RebuildIndexResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RebuildIndexResponse>): RebuildIndexResponse {
    const message = createBaseRebuildIndexResponse();
    message.documents = object.documents ?? "0";
    return message;
  },
};

function createBaseUpsertDocumentRequest(): UpsertDocumentRequest {
  return { document: undefined };
}

export const UpsertDocumentRequest: MessageFns<UpsertDocumentRequest> = {
  encode(message: UpsertDocumentRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.document !== undefined) {
      WorkspaceDocument.encode(message.document, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpsertDocumentRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpsertDocumentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.document = WorkspaceDocument.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpsertDocumentRequest {
    return { document: isSet(object.document) ? WorkspaceDocument.fromJSON(object.document) : undefined };
  },

  toJSON(message: UpsertDocumentRequest): unknown {
    const obj: any = {};
    if (message.document !== undefined) {
      obj.document = WorkspaceDocument.toJSON(message.document);
    }
    return obj;
  },

  create(base?: DeepPartial<UpsertDocumentRequest>): UpsertDocumentRequest {
    return UpsertDocumentRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpsertDocumentRequest>): UpsertDocumentRequest {
    const message = createBaseUpsertDocumentRequest();
    message.document = (object.document !== undefined && object.document !== null)
      ? WorkspaceDocument.fromPartial(object.document)
      : undefined;
    return message;
  },
};

function createBaseUpsertDocumentResponse(): UpsertDocumentResponse {
  return { documents: "0" };
}

export const UpsertDocumentResponse: MessageFns<UpsertDocumentResponse> = {
  encode(message: UpsertDocumentResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.documents !== "0") {
      writer.uint32(8).uint64(message.documents);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpsertDocumentResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpsertDocumentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.documents = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpsertDocumentResponse {
    return { documents: isSet(object.documents) ? globalThis.String(object.documents) : "0" };
  },

  toJSON(message: UpsertDocumentResponse): unknown {
    const obj: any = {};
    if (message.documents !== "0") {
      obj.documents = message.documents;
    }
    return obj;
  },

  create(base?: DeepPartial<UpsertDocumentResponse>): UpsertDocumentResponse {
    return UpsertDocumentResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpsertDocumentResponse>): UpsertDocumentResponse {
    const message = createBaseUpsertDocumentResponse();
    message.documents = object.documents ?? "0";
    return message;
  },
};

function createBaseRemoveDocumentRequest(): RemoveDocumentRequest {
  return { path: "" };
}

export const RemoveDocumentRequest: MessageFns<RemoveDocumentRequest> = {
  encode(message: RemoveDocumentRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemoveDocumentRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemoveDocumentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemoveDocumentRequest {
    return { path: isSet(object.path) ? globalThis.String(object.path) : "" };
  },

  toJSON(message: RemoveDocumentRequest): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    return obj;
  },

  create(base?: DeepPartial<RemoveDocumentRequest>): RemoveDocumentRequest {
    return RemoveDocumentRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RemoveDocumentRequest>): RemoveDocumentRequest {
    const message = createBaseRemoveDocumentRequest();
    message.path = object.path ?? "";
    return message;
  },
};

function createBaseRemoveDocumentResponse(): RemoveDocumentResponse {
  return { documents: "0" };
}

export const RemoveDocumentResponse: MessageFns<RemoveDocumentResponse> = {
  encode(message: RemoveDocumentResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.documents !== "0") {
      writer.uint32(8).uint64(message.documents);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemoveDocumentResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemoveDocumentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.documents = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemoveDocumentResponse {
    return { documents: isSet(object.documents) ? globalThis.String(object.documents) : "0" };
  },

  toJSON(message: RemoveDocumentResponse): unknown {
    const obj: any = {};
    if (message.documents !== "0") {
      obj.documents = message.documents;
    }
    return obj;
  },

  create(base?: DeepPartial<RemoveDocumentResponse>): RemoveDocumentResponse {
    return RemoveDocumentResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RemoveDocumentResponse>): RemoveDocumentResponse {
    const message = createBaseRemoveDocumentResponse();
    message.documents = object.documents ?? "0";
    return message;
  },
};

function createBaseRemovePathPrefixRequest(): RemovePathPrefixRequest {
  return { prefix: "" };
}

export const RemovePathPrefixRequest: MessageFns<RemovePathPrefixRequest> = {
  encode(message: RemovePathPrefixRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.prefix !== "") {
      writer.uint32(10).string(message.prefix);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemovePathPrefixRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemovePathPrefixRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.prefix = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemovePathPrefixRequest {
    return { prefix: isSet(object.prefix) ? globalThis.String(object.prefix) : "" };
  },

  toJSON(message: RemovePathPrefixRequest): unknown {
    const obj: any = {};
    if (message.prefix !== "") {
      obj.prefix = message.prefix;
    }
    return obj;
  },

  create(base?: DeepPartial<RemovePathPrefixRequest>): RemovePathPrefixRequest {
    return RemovePathPrefixRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RemovePathPrefixRequest>): RemovePathPrefixRequest {
    const message = createBaseRemovePathPrefixRequest();
    message.prefix = object.prefix ?? "";
    return message;
  },
};

function createBaseRemovePathPrefixResponse(): RemovePathPrefixResponse {
  return { documents: "0" };
}

export const RemovePathPrefixResponse: MessageFns<RemovePathPrefixResponse> = {
  encode(message: RemovePathPrefixResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.documents !== "0") {
      writer.uint32(8).uint64(message.documents);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemovePathPrefixResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemovePathPrefixResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.documents = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemovePathPrefixResponse {
    return { documents: isSet(object.documents) ? globalThis.String(object.documents) : "0" };
  },

  toJSON(message: RemovePathPrefixResponse): unknown {
    const obj: any = {};
    if (message.documents !== "0") {
      obj.documents = message.documents;
    }
    return obj;
  },

  create(base?: DeepPartial<RemovePathPrefixResponse>): RemovePathPrefixResponse {
    return RemovePathPrefixResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RemovePathPrefixResponse>): RemovePathPrefixResponse {
    const message = createBaseRemovePathPrefixResponse();
    message.documents = object.documents ?? "0";
    return message;
  },
};

function createBaseWorkspaceDocument(): WorkspaceDocument {
  return { path: "", title: "", content: "" };
}

export const WorkspaceDocument: MessageFns<WorkspaceDocument> = {
  encode(message: WorkspaceDocument, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.content !== "") {
      writer.uint32(26).string(message.content);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkspaceDocument {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkspaceDocument();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.content = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkspaceDocument {
    return {
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      content: isSet(object.content) ? globalThis.String(object.content) : "",
    };
  },

  toJSON(message: WorkspaceDocument): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    return obj;
  },

  create(base?: DeepPartial<WorkspaceDocument>): WorkspaceDocument {
    return WorkspaceDocument.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WorkspaceDocument>): WorkspaceDocument {
    const message = createBaseWorkspaceDocument();
    message.path = object.path ?? "";
    message.title = object.title ?? "";
    message.content = object.content ?? "";
    return message;
  },
};

function createBaseSyncRequest(): SyncRequest {
  return { workspaceInstanceId: "", open: undefined, change: undefined, close: undefined, resync: undefined };
}

export const SyncRequest: MessageFns<SyncRequest> = {
  encode(message: SyncRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workspaceInstanceId !== "") {
      writer.uint32(10).string(message.workspaceInstanceId);
    }
    if (message.open !== undefined) {
      OpenDocument.encode(message.open, writer.uint32(18).fork()).join();
    }
    if (message.change !== undefined) {
      ApplyDocumentChange.encode(message.change, writer.uint32(26).fork()).join();
    }
    if (message.close !== undefined) {
      CloseDocument.encode(message.close, writer.uint32(34).fork()).join();
    }
    if (message.resync !== undefined) {
      ResyncDocument.encode(message.resync, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SyncRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSyncRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workspaceInstanceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.open = OpenDocument.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.change = ApplyDocumentChange.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.close = CloseDocument.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.resync = ResyncDocument.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SyncRequest {
    return {
      workspaceInstanceId: isSet(object.workspaceInstanceId)
        ? globalThis.String(object.workspaceInstanceId)
        : isSet(object.workspace_instance_id)
        ? globalThis.String(object.workspace_instance_id)
        : "",
      open: isSet(object.open) ? OpenDocument.fromJSON(object.open) : undefined,
      change: isSet(object.change) ? ApplyDocumentChange.fromJSON(object.change) : undefined,
      close: isSet(object.close) ? CloseDocument.fromJSON(object.close) : undefined,
      resync: isSet(object.resync) ? ResyncDocument.fromJSON(object.resync) : undefined,
    };
  },

  toJSON(message: SyncRequest): unknown {
    const obj: any = {};
    if (message.workspaceInstanceId !== "") {
      obj.workspaceInstanceId = message.workspaceInstanceId;
    }
    if (message.open !== undefined) {
      obj.open = OpenDocument.toJSON(message.open);
    }
    if (message.change !== undefined) {
      obj.change = ApplyDocumentChange.toJSON(message.change);
    }
    if (message.close !== undefined) {
      obj.close = CloseDocument.toJSON(message.close);
    }
    if (message.resync !== undefined) {
      obj.resync = ResyncDocument.toJSON(message.resync);
    }
    return obj;
  },

  create(base?: DeepPartial<SyncRequest>): SyncRequest {
    return SyncRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SyncRequest>): SyncRequest {
    const message = createBaseSyncRequest();
    message.workspaceInstanceId = object.workspaceInstanceId ?? "";
    message.open = (object.open !== undefined && object.open !== null)
      ? OpenDocument.fromPartial(object.open)
      : undefined;
    message.change = (object.change !== undefined && object.change !== null)
      ? ApplyDocumentChange.fromPartial(object.change)
      : undefined;
    message.close = (object.close !== undefined && object.close !== null)
      ? CloseDocument.fromPartial(object.close)
      : undefined;
    message.resync = (object.resync !== undefined && object.resync !== null)
      ? ResyncDocument.fromPartial(object.resync)
      : undefined;
    return message;
  },
};

function createBaseSyncResponse(): SyncResponse {
  return { acknowledged: undefined, diagnostics: undefined, resyncRequired: undefined };
}

export const SyncResponse: MessageFns<SyncResponse> = {
  encode(message: SyncResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.acknowledged !== undefined) {
      DocumentAcknowledged.encode(message.acknowledged, writer.uint32(10).fork()).join();
    }
    if (message.diagnostics !== undefined) {
      DiagnosticsChanged.encode(message.diagnostics, writer.uint32(18).fork()).join();
    }
    if (message.resyncRequired !== undefined) {
      ResyncRequired.encode(message.resyncRequired, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SyncResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSyncResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.acknowledged = DocumentAcknowledged.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.diagnostics = DiagnosticsChanged.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.resyncRequired = ResyncRequired.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SyncResponse {
    return {
      acknowledged: isSet(object.acknowledged) ? DocumentAcknowledged.fromJSON(object.acknowledged) : undefined,
      diagnostics: isSet(object.diagnostics) ? DiagnosticsChanged.fromJSON(object.diagnostics) : undefined,
      resyncRequired: isSet(object.resyncRequired)
        ? ResyncRequired.fromJSON(object.resyncRequired)
        : isSet(object.resync_required)
        ? ResyncRequired.fromJSON(object.resync_required)
        : undefined,
    };
  },

  toJSON(message: SyncResponse): unknown {
    const obj: any = {};
    if (message.acknowledged !== undefined) {
      obj.acknowledged = DocumentAcknowledged.toJSON(message.acknowledged);
    }
    if (message.diagnostics !== undefined) {
      obj.diagnostics = DiagnosticsChanged.toJSON(message.diagnostics);
    }
    if (message.resyncRequired !== undefined) {
      obj.resyncRequired = ResyncRequired.toJSON(message.resyncRequired);
    }
    return obj;
  },

  create(base?: DeepPartial<SyncResponse>): SyncResponse {
    return SyncResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SyncResponse>): SyncResponse {
    const message = createBaseSyncResponse();
    message.acknowledged = (object.acknowledged !== undefined && object.acknowledged !== null)
      ? DocumentAcknowledged.fromPartial(object.acknowledged)
      : undefined;
    message.diagnostics = (object.diagnostics !== undefined && object.diagnostics !== null)
      ? DiagnosticsChanged.fromPartial(object.diagnostics)
      : undefined;
    message.resyncRequired = (object.resyncRequired !== undefined && object.resyncRequired !== null)
      ? ResyncRequired.fromPartial(object.resyncRequired)
      : undefined;
    return message;
  },
};

function createBaseOpenDocument(): OpenDocument {
  return { documentId: "", uri: "", version: "0", content: "" };
}

export const OpenDocument: MessageFns<OpenDocument> = {
  encode(message: OpenDocument, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.documentId !== "") {
      writer.uint32(10).string(message.documentId);
    }
    if (message.uri !== "") {
      writer.uint32(18).string(message.uri);
    }
    if (message.version !== "0") {
      writer.uint32(24).uint64(message.version);
    }
    if (message.content !== "") {
      writer.uint32(34).string(message.content);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OpenDocument {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOpenDocument();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.documentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.uri = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.version = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.content = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OpenDocument {
    return {
      documentId: isSet(object.documentId)
        ? globalThis.String(object.documentId)
        : isSet(object.document_id)
        ? globalThis.String(object.document_id)
        : "",
      uri: isSet(object.uri) ? globalThis.String(object.uri) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "0",
      content: isSet(object.content) ? globalThis.String(object.content) : "",
    };
  },

  toJSON(message: OpenDocument): unknown {
    const obj: any = {};
    if (message.documentId !== "") {
      obj.documentId = message.documentId;
    }
    if (message.uri !== "") {
      obj.uri = message.uri;
    }
    if (message.version !== "0") {
      obj.version = message.version;
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    return obj;
  },

  create(base?: DeepPartial<OpenDocument>): OpenDocument {
    return OpenDocument.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<OpenDocument>): OpenDocument {
    const message = createBaseOpenDocument();
    message.documentId = object.documentId ?? "";
    message.uri = object.uri ?? "";
    message.version = object.version ?? "0";
    message.content = object.content ?? "";
    return message;
  },
};

function createBaseApplyDocumentChange(): ApplyDocumentChange {
  return { documentId: "", baseVersion: "0", version: "0", changes: [] };
}

export const ApplyDocumentChange: MessageFns<ApplyDocumentChange> = {
  encode(message: ApplyDocumentChange, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.documentId !== "") {
      writer.uint32(10).string(message.documentId);
    }
    if (message.baseVersion !== "0") {
      writer.uint32(16).uint64(message.baseVersion);
    }
    if (message.version !== "0") {
      writer.uint32(24).uint64(message.version);
    }
    for (const v of message.changes) {
      TextEdit.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApplyDocumentChange {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApplyDocumentChange();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.documentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.baseVersion = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.version = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.changes.push(TextEdit.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ApplyDocumentChange {
    return {
      documentId: isSet(object.documentId)
        ? globalThis.String(object.documentId)
        : isSet(object.document_id)
        ? globalThis.String(object.document_id)
        : "",
      baseVersion: isSet(object.baseVersion)
        ? globalThis.String(object.baseVersion)
        : isSet(object.base_version)
        ? globalThis.String(object.base_version)
        : "0",
      version: isSet(object.version) ? globalThis.String(object.version) : "0",
      changes: globalThis.Array.isArray(object?.changes) ? object.changes.map((e: any) => TextEdit.fromJSON(e)) : [],
    };
  },

  toJSON(message: ApplyDocumentChange): unknown {
    const obj: any = {};
    if (message.documentId !== "") {
      obj.documentId = message.documentId;
    }
    if (message.baseVersion !== "0") {
      obj.baseVersion = message.baseVersion;
    }
    if (message.version !== "0") {
      obj.version = message.version;
    }
    if (message.changes?.length) {
      obj.changes = message.changes.map((e) => TextEdit.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ApplyDocumentChange>): ApplyDocumentChange {
    return ApplyDocumentChange.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ApplyDocumentChange>): ApplyDocumentChange {
    const message = createBaseApplyDocumentChange();
    message.documentId = object.documentId ?? "";
    message.baseVersion = object.baseVersion ?? "0";
    message.version = object.version ?? "0";
    message.changes = object.changes?.map((e) => TextEdit.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCloseDocument(): CloseDocument {
  return { documentId: "" };
}

export const CloseDocument: MessageFns<CloseDocument> = {
  encode(message: CloseDocument, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.documentId !== "") {
      writer.uint32(10).string(message.documentId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CloseDocument {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCloseDocument();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.documentId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CloseDocument {
    return {
      documentId: isSet(object.documentId)
        ? globalThis.String(object.documentId)
        : isSet(object.document_id)
        ? globalThis.String(object.document_id)
        : "",
    };
  },

  toJSON(message: CloseDocument): unknown {
    const obj: any = {};
    if (message.documentId !== "") {
      obj.documentId = message.documentId;
    }
    return obj;
  },

  create(base?: DeepPartial<CloseDocument>): CloseDocument {
    return CloseDocument.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CloseDocument>): CloseDocument {
    const message = createBaseCloseDocument();
    message.documentId = object.documentId ?? "";
    return message;
  },
};

function createBaseResyncDocument(): ResyncDocument {
  return { documentId: "", version: "0", content: "" };
}

export const ResyncDocument: MessageFns<ResyncDocument> = {
  encode(message: ResyncDocument, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.documentId !== "") {
      writer.uint32(10).string(message.documentId);
    }
    if (message.version !== "0") {
      writer.uint32(16).uint64(message.version);
    }
    if (message.content !== "") {
      writer.uint32(26).string(message.content);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResyncDocument {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResyncDocument();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.documentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.version = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.content = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResyncDocument {
    return {
      documentId: isSet(object.documentId)
        ? globalThis.String(object.documentId)
        : isSet(object.document_id)
        ? globalThis.String(object.document_id)
        : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "0",
      content: isSet(object.content) ? globalThis.String(object.content) : "",
    };
  },

  toJSON(message: ResyncDocument): unknown {
    const obj: any = {};
    if (message.documentId !== "") {
      obj.documentId = message.documentId;
    }
    if (message.version !== "0") {
      obj.version = message.version;
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    return obj;
  },

  create(base?: DeepPartial<ResyncDocument>): ResyncDocument {
    return ResyncDocument.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ResyncDocument>): ResyncDocument {
    const message = createBaseResyncDocument();
    message.documentId = object.documentId ?? "";
    message.version = object.version ?? "0";
    message.content = object.content ?? "";
    return message;
  },
};

function createBaseDocumentAcknowledged(): DocumentAcknowledged {
  return { documentId: "", version: "0" };
}

export const DocumentAcknowledged: MessageFns<DocumentAcknowledged> = {
  encode(message: DocumentAcknowledged, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.documentId !== "") {
      writer.uint32(10).string(message.documentId);
    }
    if (message.version !== "0") {
      writer.uint32(16).uint64(message.version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DocumentAcknowledged {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDocumentAcknowledged();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.documentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.version = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DocumentAcknowledged {
    return {
      documentId: isSet(object.documentId)
        ? globalThis.String(object.documentId)
        : isSet(object.document_id)
        ? globalThis.String(object.document_id)
        : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "0",
    };
  },

  toJSON(message: DocumentAcknowledged): unknown {
    const obj: any = {};
    if (message.documentId !== "") {
      obj.documentId = message.documentId;
    }
    if (message.version !== "0") {
      obj.version = message.version;
    }
    return obj;
  },

  create(base?: DeepPartial<DocumentAcknowledged>): DocumentAcknowledged {
    return DocumentAcknowledged.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DocumentAcknowledged>): DocumentAcknowledged {
    const message = createBaseDocumentAcknowledged();
    message.documentId = object.documentId ?? "";
    message.version = object.version ?? "0";
    return message;
  },
};

function createBaseDiagnosticsChanged(): DiagnosticsChanged {
  return { documentId: "", version: "0", diagnostics: [] };
}

export const DiagnosticsChanged: MessageFns<DiagnosticsChanged> = {
  encode(message: DiagnosticsChanged, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.documentId !== "") {
      writer.uint32(10).string(message.documentId);
    }
    if (message.version !== "0") {
      writer.uint32(16).uint64(message.version);
    }
    for (const v of message.diagnostics) {
      Diagnostic.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DiagnosticsChanged {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiagnosticsChanged();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.documentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.version = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.diagnostics.push(Diagnostic.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DiagnosticsChanged {
    return {
      documentId: isSet(object.documentId)
        ? globalThis.String(object.documentId)
        : isSet(object.document_id)
        ? globalThis.String(object.document_id)
        : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "0",
      diagnostics: globalThis.Array.isArray(object?.diagnostics)
        ? object.diagnostics.map((e: any) => Diagnostic.fromJSON(e))
        : [],
    };
  },

  toJSON(message: DiagnosticsChanged): unknown {
    const obj: any = {};
    if (message.documentId !== "") {
      obj.documentId = message.documentId;
    }
    if (message.version !== "0") {
      obj.version = message.version;
    }
    if (message.diagnostics?.length) {
      obj.diagnostics = message.diagnostics.map((e) => Diagnostic.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<DiagnosticsChanged>): DiagnosticsChanged {
    return DiagnosticsChanged.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DiagnosticsChanged>): DiagnosticsChanged {
    const message = createBaseDiagnosticsChanged();
    message.documentId = object.documentId ?? "";
    message.version = object.version ?? "0";
    message.diagnostics = object.diagnostics?.map((e) => Diagnostic.fromPartial(e)) || [];
    return message;
  },
};

function createBaseResyncRequired(): ResyncRequired {
  return { documentId: "", reason: "" };
}

export const ResyncRequired: MessageFns<ResyncRequired> = {
  encode(message: ResyncRequired, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.documentId !== "") {
      writer.uint32(10).string(message.documentId);
    }
    if (message.reason !== "") {
      writer.uint32(18).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResyncRequired {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResyncRequired();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.documentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResyncRequired {
    return {
      documentId: isSet(object.documentId)
        ? globalThis.String(object.documentId)
        : isSet(object.document_id)
        ? globalThis.String(object.document_id)
        : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
    };
  },

  toJSON(message: ResyncRequired): unknown {
    const obj: any = {};
    if (message.documentId !== "") {
      obj.documentId = message.documentId;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create(base?: DeepPartial<ResyncRequired>): ResyncRequired {
    return ResyncRequired.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ResyncRequired>): ResyncRequired {
    const message = createBaseResyncRequired();
    message.documentId = object.documentId ?? "";
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseDiagnostic(): Diagnostic {
  return { range: undefined, ruleId: "", message: "", severity: 0 };
}

export const Diagnostic: MessageFns<Diagnostic> = {
  encode(message: Diagnostic, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.range !== undefined) {
      Range.encode(message.range, writer.uint32(10).fork()).join();
    }
    if (message.ruleId !== "") {
      writer.uint32(18).string(message.ruleId);
    }
    if (message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    if (message.severity !== 0) {
      writer.uint32(32).int32(message.severity);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Diagnostic {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiagnostic();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.range = Range.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ruleId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.severity = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Diagnostic {
    return {
      range: isSet(object.range) ? Range.fromJSON(object.range) : undefined,
      ruleId: isSet(object.ruleId)
        ? globalThis.String(object.ruleId)
        : isSet(object.rule_id)
        ? globalThis.String(object.rule_id)
        : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      severity: isSet(object.severity) ? diagnosticSeverityFromJSON(object.severity) : 0,
    };
  },

  toJSON(message: Diagnostic): unknown {
    const obj: any = {};
    if (message.range !== undefined) {
      obj.range = Range.toJSON(message.range);
    }
    if (message.ruleId !== "") {
      obj.ruleId = message.ruleId;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.severity !== 0) {
      obj.severity = diagnosticSeverityToJSON(message.severity);
    }
    return obj;
  },

  create(base?: DeepPartial<Diagnostic>): Diagnostic {
    return Diagnostic.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Diagnostic>): Diagnostic {
    const message = createBaseDiagnostic();
    message.range = (object.range !== undefined && object.range !== null) ? Range.fromPartial(object.range) : undefined;
    message.ruleId = object.ruleId ?? "";
    message.message = object.message ?? "";
    message.severity = object.severity ?? 0;
    return message;
  },
};

function createBaseGetDocumentSymbolsRequest(): GetDocumentSymbolsRequest {
  return { documentId: "", documentVersion: "0" };
}

export const GetDocumentSymbolsRequest: MessageFns<GetDocumentSymbolsRequest> = {
  encode(message: GetDocumentSymbolsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.documentId !== "") {
      writer.uint32(10).string(message.documentId);
    }
    if (message.documentVersion !== "0") {
      writer.uint32(16).uint64(message.documentVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetDocumentSymbolsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetDocumentSymbolsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.documentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.documentVersion = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetDocumentSymbolsRequest {
    return {
      documentId: isSet(object.documentId)
        ? globalThis.String(object.documentId)
        : isSet(object.document_id)
        ? globalThis.String(object.document_id)
        : "",
      documentVersion: isSet(object.documentVersion)
        ? globalThis.String(object.documentVersion)
        : isSet(object.document_version)
        ? globalThis.String(object.document_version)
        : "0",
    };
  },

  toJSON(message: GetDocumentSymbolsRequest): unknown {
    const obj: any = {};
    if (message.documentId !== "") {
      obj.documentId = message.documentId;
    }
    if (message.documentVersion !== "0") {
      obj.documentVersion = message.documentVersion;
    }
    return obj;
  },

  create(base?: DeepPartial<GetDocumentSymbolsRequest>): GetDocumentSymbolsRequest {
    return GetDocumentSymbolsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetDocumentSymbolsRequest>): GetDocumentSymbolsRequest {
    const message = createBaseGetDocumentSymbolsRequest();
    message.documentId = object.documentId ?? "";
    message.documentVersion = object.documentVersion ?? "0";
    return message;
  },
};

function createBaseGetDocumentSymbolsResponse(): GetDocumentSymbolsResponse {
  return { symbols: [] };
}

export const GetDocumentSymbolsResponse: MessageFns<GetDocumentSymbolsResponse> = {
  encode(message: GetDocumentSymbolsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.symbols) {
      MarkdownDocumentSymbol.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetDocumentSymbolsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetDocumentSymbolsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.symbols.push(MarkdownDocumentSymbol.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetDocumentSymbolsResponse {
    return {
      symbols: globalThis.Array.isArray(object?.symbols)
        ? object.symbols.map((e: any) => MarkdownDocumentSymbol.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetDocumentSymbolsResponse): unknown {
    const obj: any = {};
    if (message.symbols?.length) {
      obj.symbols = message.symbols.map((e) => MarkdownDocumentSymbol.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GetDocumentSymbolsResponse>): GetDocumentSymbolsResponse {
    return GetDocumentSymbolsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetDocumentSymbolsResponse>): GetDocumentSymbolsResponse {
    const message = createBaseGetDocumentSymbolsResponse();
    message.symbols = object.symbols?.map((e) => MarkdownDocumentSymbol.fromPartial(e)) || [];
    return message;
  },
};

function createBaseMarkdownDocumentSymbol(): MarkdownDocumentSymbol {
  return { name: "", kind: 0, level: 0, slug: "", range: undefined };
}

export const MarkdownDocumentSymbol: MessageFns<MarkdownDocumentSymbol> = {
  encode(message: MarkdownDocumentSymbol, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.kind !== 0) {
      writer.uint32(16).uint32(message.kind);
    }
    if (message.level !== 0) {
      writer.uint32(24).uint32(message.level);
    }
    if (message.slug !== "") {
      writer.uint32(34).string(message.slug);
    }
    if (message.range !== undefined) {
      Range.encode(message.range, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MarkdownDocumentSymbol {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarkdownDocumentSymbol();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.kind = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.level = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.slug = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.range = Range.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarkdownDocumentSymbol {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      kind: isSet(object.kind) ? globalThis.Number(object.kind) : 0,
      level: isSet(object.level) ? globalThis.Number(object.level) : 0,
      slug: isSet(object.slug) ? globalThis.String(object.slug) : "",
      range: isSet(object.range) ? Range.fromJSON(object.range) : undefined,
    };
  },

  toJSON(message: MarkdownDocumentSymbol): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.kind !== 0) {
      obj.kind = Math.round(message.kind);
    }
    if (message.level !== 0) {
      obj.level = Math.round(message.level);
    }
    if (message.slug !== "") {
      obj.slug = message.slug;
    }
    if (message.range !== undefined) {
      obj.range = Range.toJSON(message.range);
    }
    return obj;
  },

  create(base?: DeepPartial<MarkdownDocumentSymbol>): MarkdownDocumentSymbol {
    return MarkdownDocumentSymbol.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MarkdownDocumentSymbol>): MarkdownDocumentSymbol {
    const message = createBaseMarkdownDocumentSymbol();
    message.name = object.name ?? "";
    message.kind = object.kind ?? 0;
    message.level = object.level ?? 0;
    message.slug = object.slug ?? "";
    message.range = (object.range !== undefined && object.range !== null) ? Range.fromPartial(object.range) : undefined;
    return message;
  },
};

function createBaseGetLinksRequest(): GetLinksRequest {
  return { documentId: "", documentVersion: "0" };
}

export const GetLinksRequest: MessageFns<GetLinksRequest> = {
  encode(message: GetLinksRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.documentId !== "") {
      writer.uint32(10).string(message.documentId);
    }
    if (message.documentVersion !== "0") {
      writer.uint32(16).uint64(message.documentVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetLinksRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetLinksRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.documentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.documentVersion = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetLinksRequest {
    return {
      documentId: isSet(object.documentId)
        ? globalThis.String(object.documentId)
        : isSet(object.document_id)
        ? globalThis.String(object.document_id)
        : "",
      documentVersion: isSet(object.documentVersion)
        ? globalThis.String(object.documentVersion)
        : isSet(object.document_version)
        ? globalThis.String(object.document_version)
        : "0",
    };
  },

  toJSON(message: GetLinksRequest): unknown {
    const obj: any = {};
    if (message.documentId !== "") {
      obj.documentId = message.documentId;
    }
    if (message.documentVersion !== "0") {
      obj.documentVersion = message.documentVersion;
    }
    return obj;
  },

  create(base?: DeepPartial<GetLinksRequest>): GetLinksRequest {
    return GetLinksRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetLinksRequest>): GetLinksRequest {
    const message = createBaseGetLinksRequest();
    message.documentId = object.documentId ?? "";
    message.documentVersion = object.documentVersion ?? "0";
    return message;
  },
};

function createBaseGetLinksResponse(): GetLinksResponse {
  return { links: [] };
}

export const GetLinksResponse: MessageFns<GetLinksResponse> = {
  encode(message: GetLinksResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.links) {
      MarkdownLink.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetLinksResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetLinksResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.links.push(MarkdownLink.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetLinksResponse {
    return {
      links: globalThis.Array.isArray(object?.links) ? object.links.map((e: any) => MarkdownLink.fromJSON(e)) : [],
    };
  },

  toJSON(message: GetLinksResponse): unknown {
    const obj: any = {};
    if (message.links?.length) {
      obj.links = message.links.map((e) => MarkdownLink.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GetLinksResponse>): GetLinksResponse {
    return GetLinksResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetLinksResponse>): GetLinksResponse {
    const message = createBaseGetLinksResponse();
    message.links = object.links?.map((e) => MarkdownLink.fromPartial(e)) || [];
    return message;
  },
};

function createBaseMarkdownLink(): MarkdownLink {
  return { sourceDocumentId: "", text: "", target: "", range: undefined, isExternal: false };
}

export const MarkdownLink: MessageFns<MarkdownLink> = {
  encode(message: MarkdownLink, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sourceDocumentId !== "") {
      writer.uint32(10).string(message.sourceDocumentId);
    }
    if (message.text !== "") {
      writer.uint32(18).string(message.text);
    }
    if (message.target !== "") {
      writer.uint32(26).string(message.target);
    }
    if (message.range !== undefined) {
      Range.encode(message.range, writer.uint32(34).fork()).join();
    }
    if (message.isExternal !== false) {
      writer.uint32(40).bool(message.isExternal);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MarkdownLink {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarkdownLink();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sourceDocumentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.target = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.range = Range.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.isExternal = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarkdownLink {
    return {
      sourceDocumentId: isSet(object.sourceDocumentId)
        ? globalThis.String(object.sourceDocumentId)
        : isSet(object.source_document_id)
        ? globalThis.String(object.source_document_id)
        : "",
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      target: isSet(object.target) ? globalThis.String(object.target) : "",
      range: isSet(object.range) ? Range.fromJSON(object.range) : undefined,
      isExternal: isSet(object.isExternal)
        ? globalThis.Boolean(object.isExternal)
        : isSet(object.is_external)
        ? globalThis.Boolean(object.is_external)
        : false,
    };
  },

  toJSON(message: MarkdownLink): unknown {
    const obj: any = {};
    if (message.sourceDocumentId !== "") {
      obj.sourceDocumentId = message.sourceDocumentId;
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.target !== "") {
      obj.target = message.target;
    }
    if (message.range !== undefined) {
      obj.range = Range.toJSON(message.range);
    }
    if (message.isExternal !== false) {
      obj.isExternal = message.isExternal;
    }
    return obj;
  },

  create(base?: DeepPartial<MarkdownLink>): MarkdownLink {
    return MarkdownLink.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MarkdownLink>): MarkdownLink {
    const message = createBaseMarkdownLink();
    message.sourceDocumentId = object.sourceDocumentId ?? "";
    message.text = object.text ?? "";
    message.target = object.target ?? "";
    message.range = (object.range !== undefined && object.range !== null) ? Range.fromPartial(object.range) : undefined;
    message.isExternal = object.isExternal ?? false;
    return message;
  },
};

function createBaseSearchRequest(): SearchRequest {
  return { query: "", limit: 0 };
}

export const SearchRequest: MessageFns<SearchRequest> = {
  encode(message: SearchRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== "") {
      writer.uint32(10).string(message.query);
    }
    if (message.limit !== 0) {
      writer.uint32(16).uint32(message.limit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.limit = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchRequest {
    return {
      query: isSet(object.query) ? globalThis.String(object.query) : "",
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
    };
  },

  toJSON(message: SearchRequest): unknown {
    const obj: any = {};
    if (message.query !== "") {
      obj.query = message.query;
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    return obj;
  },

  create(base?: DeepPartial<SearchRequest>): SearchRequest {
    return SearchRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SearchRequest>): SearchRequest {
    const message = createBaseSearchRequest();
    message.query = object.query ?? "";
    message.limit = object.limit ?? 0;
    return message;
  },
};

function createBaseSearchResponse(): SearchResponse {
  return { results: [], done: false };
}

export const SearchResponse: MessageFns<SearchResponse> = {
  encode(message: SearchResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.results) {
      SearchResult.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.done !== false) {
      writer.uint32(16).bool(message.done);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.results.push(SearchResult.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.done = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchResponse {
    return {
      results: globalThis.Array.isArray(object?.results)
        ? object.results.map((e: any) => SearchResult.fromJSON(e))
        : [],
      done: isSet(object.done) ? globalThis.Boolean(object.done) : false,
    };
  },

  toJSON(message: SearchResponse): unknown {
    const obj: any = {};
    if (message.results?.length) {
      obj.results = message.results.map((e) => SearchResult.toJSON(e));
    }
    if (message.done !== false) {
      obj.done = message.done;
    }
    return obj;
  },

  create(base?: DeepPartial<SearchResponse>): SearchResponse {
    return SearchResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SearchResponse>): SearchResponse {
    const message = createBaseSearchResponse();
    message.results = object.results?.map((e) => SearchResult.fromPartial(e)) || [];
    message.done = object.done ?? false;
    return message;
  },
};

function createBaseSearchResult(): SearchResult {
  return {
    documentId: "",
    path: "",
    title: "",
    line: 0,
    column: 0,
    endColumn: 0,
    snippet: "",
    snippetHighlights: [],
    score: 0,
  };
}

export const SearchResult: MessageFns<SearchResult> = {
  encode(message: SearchResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.documentId !== "") {
      writer.uint32(10).string(message.documentId);
    }
    if (message.path !== "") {
      writer.uint32(18).string(message.path);
    }
    if (message.title !== "") {
      writer.uint32(26).string(message.title);
    }
    if (message.line !== 0) {
      writer.uint32(32).uint32(message.line);
    }
    if (message.column !== 0) {
      writer.uint32(40).uint32(message.column);
    }
    if (message.endColumn !== 0) {
      writer.uint32(48).uint32(message.endColumn);
    }
    if (message.snippet !== "") {
      writer.uint32(58).string(message.snippet);
    }
    for (const v of message.snippetHighlights) {
      SearchHighlight.encode(v!, writer.uint32(66).fork()).join();
    }
    if (message.score !== 0) {
      writer.uint32(77).float(message.score);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.documentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.line = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.column = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.endColumn = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.snippet = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.snippetHighlights.push(SearchHighlight.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 77) {
            break;
          }

          message.score = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchResult {
    return {
      documentId: isSet(object.documentId)
        ? globalThis.String(object.documentId)
        : isSet(object.document_id)
        ? globalThis.String(object.document_id)
        : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      line: isSet(object.line) ? globalThis.Number(object.line) : 0,
      column: isSet(object.column) ? globalThis.Number(object.column) : 0,
      endColumn: isSet(object.endColumn)
        ? globalThis.Number(object.endColumn)
        : isSet(object.end_column)
        ? globalThis.Number(object.end_column)
        : 0,
      snippet: isSet(object.snippet) ? globalThis.String(object.snippet) : "",
      snippetHighlights: globalThis.Array.isArray(object?.snippetHighlights)
        ? object.snippetHighlights.map((e: any) => SearchHighlight.fromJSON(e))
        : globalThis.Array.isArray(object?.snippet_highlights)
        ? object.snippet_highlights.map((e: any) => SearchHighlight.fromJSON(e))
        : [],
      score: isSet(object.score) ? globalThis.Number(object.score) : 0,
    };
  },

  toJSON(message: SearchResult): unknown {
    const obj: any = {};
    if (message.documentId !== "") {
      obj.documentId = message.documentId;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.line !== 0) {
      obj.line = Math.round(message.line);
    }
    if (message.column !== 0) {
      obj.column = Math.round(message.column);
    }
    if (message.endColumn !== 0) {
      obj.endColumn = Math.round(message.endColumn);
    }
    if (message.snippet !== "") {
      obj.snippet = message.snippet;
    }
    if (message.snippetHighlights?.length) {
      obj.snippetHighlights = message.snippetHighlights.map((e) => SearchHighlight.toJSON(e));
    }
    if (message.score !== 0) {
      obj.score = message.score;
    }
    return obj;
  },

  create(base?: DeepPartial<SearchResult>): SearchResult {
    return SearchResult.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SearchResult>): SearchResult {
    const message = createBaseSearchResult();
    message.documentId = object.documentId ?? "";
    message.path = object.path ?? "";
    message.title = object.title ?? "";
    message.line = object.line ?? 0;
    message.column = object.column ?? 0;
    message.endColumn = object.endColumn ?? 0;
    message.snippet = object.snippet ?? "";
    message.snippetHighlights = object.snippetHighlights?.map((e) => SearchHighlight.fromPartial(e)) || [];
    message.score = object.score ?? 0;
    return message;
  },
};

function createBaseSearchHighlight(): SearchHighlight {
  return { start: 0, end: 0 };
}

export const SearchHighlight: MessageFns<SearchHighlight> = {
  encode(message: SearchHighlight, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.start !== 0) {
      writer.uint32(8).uint32(message.start);
    }
    if (message.end !== 0) {
      writer.uint32(16).uint32(message.end);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchHighlight {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchHighlight();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.start = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.end = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchHighlight {
    return {
      start: isSet(object.start) ? globalThis.Number(object.start) : 0,
      end: isSet(object.end) ? globalThis.Number(object.end) : 0,
    };
  },

  toJSON(message: SearchHighlight): unknown {
    const obj: any = {};
    if (message.start !== 0) {
      obj.start = Math.round(message.start);
    }
    if (message.end !== 0) {
      obj.end = Math.round(message.end);
    }
    return obj;
  },

  create(base?: DeepPartial<SearchHighlight>): SearchHighlight {
    return SearchHighlight.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SearchHighlight>): SearchHighlight {
    const message = createBaseSearchHighlight();
    message.start = object.start ?? 0;
    message.end = object.end ?? 0;
    return message;
  },
};

function createBaseTextEdit(): TextEdit {
  return { range: undefined, text: "" };
}

export const TextEdit: MessageFns<TextEdit> = {
  encode(message: TextEdit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.range !== undefined) {
      Range.encode(message.range, writer.uint32(10).fork()).join();
    }
    if (message.text !== "") {
      writer.uint32(18).string(message.text);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TextEdit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTextEdit();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.range = Range.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.text = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TextEdit {
    return {
      range: isSet(object.range) ? Range.fromJSON(object.range) : undefined,
      text: isSet(object.text) ? globalThis.String(object.text) : "",
    };
  },

  toJSON(message: TextEdit): unknown {
    const obj: any = {};
    if (message.range !== undefined) {
      obj.range = Range.toJSON(message.range);
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    return obj;
  },

  create(base?: DeepPartial<TextEdit>): TextEdit {
    return TextEdit.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TextEdit>): TextEdit {
    const message = createBaseTextEdit();
    message.range = (object.range !== undefined && object.range !== null) ? Range.fromPartial(object.range) : undefined;
    message.text = object.text ?? "";
    return message;
  },
};

function createBaseRange(): Range {
  return { start: undefined, end: undefined };
}

export const Range: MessageFns<Range> = {
  encode(message: Range, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.start !== undefined) {
      Position.encode(message.start, writer.uint32(10).fork()).join();
    }
    if (message.end !== undefined) {
      Position.encode(message.end, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Range {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRange();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.start = Position.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.end = Position.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Range {
    return {
      start: isSet(object.start) ? Position.fromJSON(object.start) : undefined,
      end: isSet(object.end) ? Position.fromJSON(object.end) : undefined,
    };
  },

  toJSON(message: Range): unknown {
    const obj: any = {};
    if (message.start !== undefined) {
      obj.start = Position.toJSON(message.start);
    }
    if (message.end !== undefined) {
      obj.end = Position.toJSON(message.end);
    }
    return obj;
  },

  create(base?: DeepPartial<Range>): Range {
    return Range.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Range>): Range {
    const message = createBaseRange();
    message.start = (object.start !== undefined && object.start !== null)
      ? Position.fromPartial(object.start)
      : undefined;
    message.end = (object.end !== undefined && object.end !== null) ? Position.fromPartial(object.end) : undefined;
    return message;
  },
};

function createBasePosition(): Position {
  return { line: 0, character: 0 };
}

export const Position: MessageFns<Position> = {
  encode(message: Position, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.line !== 0) {
      writer.uint32(8).uint32(message.line);
    }
    if (message.character !== 0) {
      writer.uint32(16).uint32(message.character);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Position {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePosition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.line = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.character = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Position {
    return {
      line: isSet(object.line) ? globalThis.Number(object.line) : 0,
      character: isSet(object.character) ? globalThis.Number(object.character) : 0,
    };
  },

  toJSON(message: Position): unknown {
    const obj: any = {};
    if (message.line !== 0) {
      obj.line = Math.round(message.line);
    }
    if (message.character !== 0) {
      obj.character = Math.round(message.character);
    }
    return obj;
  },

  create(base?: DeepPartial<Position>): Position {
    return Position.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Position>): Position {
    const message = createBasePosition();
    message.line = object.line ?? 0;
    message.character = object.character ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
